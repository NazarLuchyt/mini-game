import {Routes} from '@angular/router';
import {FieldComponent} from './components/field/field.component';

export const gameModuleRoutes: Routes = [
    {
        path: '',
        component: FieldComponent,
    },
];
